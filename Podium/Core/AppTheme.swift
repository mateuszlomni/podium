import SwiftUI

enum AppTheme {
    static let background = Color(red: 0.025, green: 0.03, blue: 0.04)
    static let panel = Color.white.opacity(0.055)
    static let panelStrong = Color.white.opacity(0.085)
    static let primaryText = Color.white
    static let secondaryText = Color.white.opacity(0.62)
    static let accent = Color(red: 0.66, green: 0.82, blue: 1.0)
    static let spotify = Color(red: 0.12, green: 0.84, blue: 0.38)

    static let horizontalPadding: CGFloat = 22
}
